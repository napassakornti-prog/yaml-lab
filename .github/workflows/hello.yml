name: Hello Workflow

on:
  push:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
      - name: Say hello
        run: echo "สวัสดี Github Actions!"

      - name: Show date
        run: date

      - name: Show machine info
        run: uname -a